[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "uacos"
version = "4.1.0b4"
description = "Local-first repo intelligence, context compression, patch safety, MCP, and evidence tooling for AI coding workflows."
readme = "README.md"
license = "MIT"
requires-python = ">=3.9"
authors = [
  { name = "UACOS maintainers" }
]
keywords = ["code-intelligence", "coding-agents", "context-compression", "developer-tools", "llm", "local-first", "mcp", "patch-safety", "release-gate"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Topic :: Software Development :: Quality Assurance",
  "Topic :: Software Development :: Libraries :: Python Modules",
]

[project.optional-dependencies]
test = ["pytest>=7"]
dev = ["pytest>=7", "build>=1.0"]

[project.scripts]
uacos = "uacos.cli:main"
uacos-flow = "uacos.flow_cli:main"

[project.urls]
Homepage = "https://github.com/caotiensinh/uacos"
Documentation = "https://github.com/caotiensinh/uacos/tree/main/docs"
Issues = "https://github.com/caotiensinh/uacos/issues"

[tool.setuptools.packages.find]
where = ["."]
include = ["uacos*"]
exclude = ["uacos_phase35_full*", "test_out*", "out*", "_uacos*"]
